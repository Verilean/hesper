# Kernel stub overview (llama.cpp/ggml/src/ggml-cuda)

Auto-generated by `lake exe stub-extract --report`.

Found **10** interesting kernels (template / __launch_bounds__ / smem / characteristic loops / __syncthreads のいずれかを持つもの).

Columns: `occ` = `__launch_bounds__(max_threads, min_blocks_per_sm)`. `block` = first launch site's block dim.

## Summary

| kernel | file | template | occ (max_thr / min_blk) | block | smem | loops | syncs | sites |
|---|---|---|---|---|---|---|---|---|
| `upscale_f32` | `upscale.cu` | — | — | — | — | — | — | — |
| `upscale_f32_bilinear` | `upscale.cu` | — | — | `CUDA_UPSCALE_BLOCK_SIZE` | — | — | — | 1 site |
| `upscale_f32_bilinear_antialias` | `upscale.cu` | — | — | `CUDA_UPSCALE_BLOCK_SIZE` | — | — | — | 1 site |
| `upscale_f32_bicubic` | `upscale.cu` | — | — | — | — | — | — | — |
| `conv2d_dw_kernel` | `conv2d-dw.cu` | `typename T, typename Layout` | — | `CUDA_CONV2D_DW_BLOCK_SIZE` | — | — | — | 2 site |
| `pad_f32` | `pad.cu` | — | — | `CUDA_PAD_BLOCK_SIZE` | — | — | — | 1 site |
| `conv2d_transpose_kernel` | `conv2d-transpose.cu` | `typename kernel_t` | — | `CUDA_CONV2D_TRANSPOSE_BLOCK_SIZE` | — | — | — | 2 site |
| `im2col_kernel` | `im2col.cu` | `typename T` | — | `MIN(IC_KH_KW, CUDA_IM2COL_BLOCK_SIZE)` | — | — | — | 1 site |
| `im2col_3d_kernel` | `im2col.cu` | `typename T` | — | `MIN(IC_KD_KH_KW, CUDA_IM2COL_BLOCK_SIZE)` | — | — | — | 1 site |
| `conv2d_kernel` | `conv2d.cu` | `typename T, typename Layout` | — | `CUDA_CONV2D_BLOCK_SIZE` | — | — | — | 1 site |

## Per-kernel detail

### `upscale.cu`

```
=== upscale_f32 (upscale.cu:3) ===
  attrs:    __global__ static
```

```
=== upscale_f32_bilinear (upscale.cu:25) ===
  attrs:    __global__ static
  launches:
    grid=(dst_size + CUDA_UPSCALE_BLOCK_SIZE - 1) / CUDA_UPSCALE_BLOCK_SIZE  block=CUDA_UPSCALE_BLOCK_SIZE  smem=0  stream=stream
```

```
=== upscale_f32_bilinear_antialias (upscale.cu:86) ===
  attrs:    __global__ static
  launches:
    grid=(dst_size + CUDA_UPSCALE_BLOCK_SIZE - 1) / CUDA_UPSCALE_BLOCK_SIZE  block=CUDA_UPSCALE_BLOCK_SIZE  smem=0  stream=stream
  callees (1):
    triangle_filter ×2
```

```
=== upscale_f32_bicubic (upscale.cu:170) ===
  attrs:    __device__ static __global__
  callees (2):
    bicubic ×5
    load ×4
```

### `conv2d-dw.cu`

```
=== conv2d_dw_kernel (conv2d-dw.cu:82) ===
  attrs:    __device__ static __global__
  template (2):
    T : typename
    Layout : typename
  launches:
    grid=(total + CUDA_CONV2D_DW_BLOCK_SIZE - 1) / CUDA_CONV2D_DW_BLOCK_SIZE  block=CUDA_CONV2D_DW_BLOCK_SIZE  smem=0  stream=st
    grid=(total + CUDA_CONV2D_DW_BLOCK_SIZE - 1) / CUDA_CONV2D_DW_BLOCK_SIZE  block=CUDA_CONV2D_DW_BLOCK_SIZE  smem=0  stream=st
  callees (6):
    unpack_indices ×1
    calculate_kernel_bounds ×1
    calculate_input_coord ×2
    input_index ×1
    kernel_index ×1
    output_index ×1
```

### `pad.cu`

```
=== pad_f32 (pad.cu:10) ===
  attrs:    __device__ __forceinline__ inline __global__ static
  launches:
    grid=num_blocks, ne1, ne2 * ne3  block=CUDA_PAD_BLOCK_SIZE  smem=0  stream=stream
  callees (1):
    wrap_around ×4
```

### `conv2d-transpose.cu`

```
=== conv2d_transpose_kernel (conv2d-transpose.cu:5) ===
  attrs:    __global__ static
  template (1):
    kernel_t : typename
  launches:
    grid=(total + CUDA_CONV2D_TRANSPOSE_BLOCK_SIZE - 1) / CUDA_CONV2D_TRANSPOSE_BLOCK_SIZE  block=CUDA_CONV2D_TRANSPOSE_BLOCK_SIZE  smem=0  stream=st
    grid=(total + CUDA_CONV2D_TRANSPOSE_BLOCK_SIZE - 1) / CUDA_CONV2D_TRANSPOSE_BLOCK_SIZE  block=CUDA_CONV2D_TRANSPOSE_BLOCK_SIZE  smem=0  stream=st
  callees (1):
    ggml_cuda_cast ×1
```

### `im2col.cu`

```
=== im2col_kernel (im2col.cu:6) ===
  attrs:    __global__ static
  template (1):
    T : typename
  launches:
    grid=num_blocks, OW, MIN(N_OH, MAX_GRIDDIM_Z)  block=MIN(IC_KH_KW, CUDA_IM2COL_BLOCK_SIZE)  smem=0  stream=stream
```

```
=== im2col_3d_kernel (im2col.cu:118) ===
  attrs:    __global__ static
  template (1):
    T : typename
  launches:
    grid=num_blocks, OW, MIN(N_OD_OH, MAX_GRIDDIM_Z)  block=MIN(IC_KD_KH_KW, CUDA_IM2COL_BLOCK_SIZE)  smem=0  stream=stream
```

### `conv2d.cu`

```
=== conv2d_kernel (conv2d.cu:73) ===
  attrs:    __device__ static __global__
  template (2):
    T : typename
    Layout : typename
  launches:
    grid=(P.TOTAL + CUDA_CONV2D_BLOCK_SIZE - 1) / CUDA_CONV2D_BLOCK_SIZE  block=CUDA_CONV2D_BLOCK_SIZE  smem=0  stream=st
  callees (7):
    unpack_indices ×1
    calculate_kernel_bounds ×1
    calculate_input_coord ×2
    input_index ×1
    kernel_index ×1
    ggml_cuda_cast ×1
    output_index ×1
```

