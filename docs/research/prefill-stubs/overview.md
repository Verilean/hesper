# Kernel stub overview (llama.cpp/ggml/src/ggml-cuda)

Auto-generated by `lake exe stub-extract --report`.

Found **52** interesting kernels (template / __launch_bounds__ / smem / characteristic loops / __syncthreads のいずれかを持つもの).

Columns: `occ` = `__launch_bounds__(max_threads, min_blocks_per_sm)`. `block` = first launch site's block dim.

## Summary

| kernel | file | template | occ (max_thr / min_blk) | block | smem | loops | syncs | sites |
|---|---|---|---|---|---|---|---|---|
| `dequantize_block_q8_0_f16` | `convert.cu` | `bool need_check` | — | `WARP_SIZE` | 1 decl | — | 1× | 2 site |
| `cumsum_cub_kernel` | `cumsum.cu` | — | — | `CUDA_CUMSUM_BLOCK_SIZE` | 2 decl | — | 3× | 1 site |
| `cumsum_kernel` | `cumsum.cu` | — | — | `block_size, 1, 1` | 1 decl | — | 4× | 1 site |
| `solve_tri_f32_fast` | `solve_tri.cu` | `int n_template, int k_template` | — | — | 1 decl | — | 1× | — |
| `mul_mat_vec_f` | `mmvf.cu` | `typename T, typename type_acc, int ncols_dst, int block_size, bool has_fusion = false, bool is_multi_token_id = false` | — | `block_dims` | 1 decl | — | 3× | 2 site |
| `cpy_scalar_transpose` | `cpy.cu` | `typename T` | — | `CUDA_CPY_TILE_DIM_2D, CUDA_CPY_BLOCK_ROWS, 1` | 1 decl | — | 1× | 1 site |
| `ssm_conv_long_token_f32` | `ssm-conv.cu` | `bool apply_silu, size_t split_d_inner, size_t d_conv, int64_t split_n_t` | — | `128` | 1 decl | — | 1× | 1 site |
| `mm_ids_helper` | `mmid.cu` | `int n_expert_used_template` | `ggml_cuda_get_physical_warp_size()` / 1 | `warp_size, 1, 1` | 1 decl | 4 loop | — | 1 site |
| `flash_attn_ext_vec` | `fattn-vec.cuh` | — | `ggml_cuda_fattn_vec_get_nthreads_device()` / 1 | `warp_size, nwarps, 1` | 4 decl | 3 loop | 6× | 1 site |
| `k_argsort_f32_i32` | `argsort.cu` | — | — | — | 1 decl | — | 2× | — |
| `rwkv_wkv_f32` | `wkv.cu` | `int block_size` | — | `C / H` | 1 decl | — | 4× | 2 site |
| `rwkv_wkv7_f32` | `wkv.cu` | `int block_size` | — | `C / H` | 1 decl | — | 2× | 2 site |
| `flash_attn_mask_to_KV_max` | `fattn-common.cuh` | `int ncols1` | `FATTN_KQ_STRIDE/2` / 1 | `FATTN_KQ_STRIDE/2, 1, 1` | 1 decl | — | 3× | 1 site |
| `flash_attn_stream_k_fixup` | `fattn-common.cuh` | — | `D` / 1 | — | — | — | — | — |
| `flash_attn_combine_results` | `fattn-common.cuh` | — | `D` / 1 | — | 1 decl | — | 1× | — |
| `gated_delta_net_cuda` | `gated_delta_net.cu` | `int S_v, bool KDA` | `(ggml_cuda_get_physical_warp_size() < S_v ? ggml_cuda_get_physical_warp_size() : S_v) * 4, 2` | `block_dims` | — | — | — | 4 site |
| `flash_attn_ext_f16_load_tile` | `fattn-mma-f16.cuh` | — | — | — | — | 2 loop | — | — |
| `flash_attn_ext_f16_load_mask` | `fattn-mma-f16.cuh` | — | — | — | — | 4 loop | — | — |
| `flash_attn_ext_f16_process_tile` | `fattn-mma-f16.cuh` | — | — | — | 1 decl | 6 loop | 12× | — |
| `flash_attn_ext_f16` | `fattn-mma-f16.cuh` | — | `ggml_cuda_fattn_mma_get_nthreads(DKQ, DV, ncols1*ncols2)` / ggml_cuda_fattn_mma_get_occupancy(DKQ, DV, ncols1*ncols2) | `warp_size, nwarps, 1` | — | — | — | 1 site |
| `mul_mat_q_process_tile` | `mmq.cuh` | `ggml_type type, int mmq_x, bool need_check, bool fixup` | — | — | 1 decl | 3 loop | 4× | — |
| `mul_mat_q` | `mmq.cuh` | `ggml_type type, int mmq_x, bool need_check` | `ggml_cuda_get_physical_warp_size()*mmq_get_nwarps_device()` / 2 | `warp_size, nwarps, 1` | 1 decl | 4 loop | 7× | 4 site |
| `mul_mat_q_stream_k_fixup` | `mmq.cuh` | `ggml_type type, int mmq_x, bool need_check` | — | `warp_size, nwarps, 1` | 1 decl | 7 loop | 1× | 2 site |
| `ssm_scan_f32` | `ssm-scan.cu` | `size_t splitD, size_t N, size_t L_template` | `splitD` / 1 | `128` | 3 decl | — | 1× | 9 site |
| `ssm_scan_f32_group` | `ssm-scan.cu` | `int c_factor, int d_state` | `d_state` / 1 | `threads` | — | — | — | 2 site |
| `topk_moe_cuda` | `topk-moe.cu` | `int n_experts, bool has_bias` | `4 * WARP_SIZE` / 1 | `block_dims` | — | — | — | 11 site |
| `argmax_f32` | `argmax.cu` | — | — | `num_threads, 1, 1` | 2 decl | — | 1× | 1 site |
| `reduce_rows_f32` | `reduce_rows.cuh` | `bool norm` | — | `512, 1, 1` | 1 decl | — | — | 6 site |
| `norm_f32` | `norm.cu` | `int block_size` | — | `WARP_SIZE, 1, 1` | 1 decl | — | — | 12 site |
| `group_norm_f32` | `norm.cu` | `int block_size` | — | — | 1 decl | — | — | — |
| `rms_norm_f32` | `norm.cu` | `int block_size, bool do_multiply = false, bool do_add = false` | — | — | 1 decl | — | — | — |
| `rms_norm_back_f32` | `norm.cu` | `int block_size` | — | `WARP_SIZE, 1, 1` | 2 decl | — | 1× | 2 site |
| `l2_norm_f32` | `norm.cu` | `int block_size` | — | — | 1 decl | — | 1× | — |
| `l2_norm_f32` | `norm.cu` | `int block_size` | — | — | 1 decl | — | — | — |
| `flash_attn_ext_f16` | `fattn-wmma-f16.cu` | — | `nwarps*ggml_cuda_get_physical_warp_size()` / 1 | `warp_size, nwarps, 1` | 2 decl | 20 loop | 8× | 1 site |
| `mul_mat_f` | `mmf.cuh` | `typename T, int rows_per_block, int cols_per_block, int nwarps, bool has_ids` | `ggml_cuda_get_physical_warp_size()*nwarps` / 1 | `block_dims` | — | — | — | 2 site |
| `mul_mat_f_ids` | `mmf.cuh` | `typename T, int rows_per_block, int cols_per_block, int nwarps` | `ggml_cuda_get_physical_warp_size()*nwarps` / 1 | `block_dims` | — | — | — | 1 site |
| `mul_mat_vec_q` | `mmvq.cu` | `ggml_type type, int ncols_dst, bool has_fusion, bool small_k = false` | `calc_nwarps(type, ncols_dst, get_device_table_id())*ggml_cuda_get_physical_warp_size()` / 1 | `warp_size, nwarps, 1` | 2 decl | 2 loop | 1× | 2 site |
| `mul_mat_vec_q_moe` | `mmvq.cu` | `ggml_type type, int c_rows_per_block` | `get_mmvq_mmid_max_batch_for_device<type>()*ggml_cuda_get_physical_warp_size()` / 1 | `warp_size, ncols_dst` | — | 1 loop | — | 1 site |
| `soft_max_f32` | `softmax.cu` | `bool use_shared, int ncols_template, int block_size_template, typename T` | — | `block_dims` | 1 decl | — | — | 2 site |
| `soft_max_f32_parallelize_cols_single_row` | `softmax.cu` | — | — | — | 1 decl | — | — | — |
| `soft_max_f32_parallelize_cols` | `softmax.cu` | — | `8*WARP_SIZE` / 1 | — | — | — | — | — |
| `concat_f32_non_cont` | `concat.cu` | `int dim` | `CUDA_CONCAT_BLOCK_SIZE` | `CUDA_CONCAT_BLOCK_SIZE` | — | — | — | 1 site |
| `quantize_q8_1` | `quantize.cu` | — | `CUDA_QUANTIZE_BLOCK_SIZE` / 1 | `CUDA_QUANTIZE_BLOCK_SIZE, 1, 1` | — | — | — | 1 site |
| `block_reduce` | `common.cuh` | `block_reduce_method reduce_method_t, const unsigned int block_size_template = 0, typename T` | — | — | — | — | 1× | — |
| `gated_linear_attn_f32` | `gla.cu` | — | — | `C / H` | 1 decl | — | 2× | 2 site |
| `cross_entropy_loss_f32` | `cross-entropy-loss.cu` | `bool use_shared` | — | `WARP_SIZE, 1, 1` | 1 decl | — | — | 2 site |
| `cross_entropy_loss_back_f32` | `cross-entropy-loss.cu` | `bool use_shared` | — | `WARP_SIZE, 1, 1` | 1 decl | — | — | 2 site |
| `flash_attn_tile_load_tile` | `fattn-tile.cuh` | — | — | — | — | 1 loop | — | — |
| `flash_attn_tile_load_tile` | `fattn-tile.cuh` | — | — | — | — | 1 loop | — | — |
| `flash_attn_tile_iter` | `fattn-tile.cuh` | `int warp_size, int nwarps, int ncols1, int ncols2, int DKQ, int DV, int nbatch_fa, int nbatch_K,` | — | — | 1 decl | 9 loop | 4× | — |
| `flash_attn_tile` | `fattn-tile.cuh` | — | `ggml_cuda_fattn_tile_get_nthreads(DKQ, DV, ncols1*ncols2)` / ggml_cuda_fattn_tile_get_occupancy(DKQ, DV, ncols1*ncols2) | `warp_size, nwarps, 1` | 6 decl | 10 loop | 2× | 1 site |

## Per-kernel detail

### `convert.cu`

```
=== dequantize_block_q8_0_f16 (convert.cu:44) ===
  attrs:    __global__ static
  template (1):
    need_check : bool
  launches:
    grid=(k + CUDA_Q8_0_NE_ALIGN - 1) / CUDA_Q8_0_NE_ALIGN  block=WARP_SIZE  smem=0  stream=stream
    grid=(k + CUDA_Q8_0_NE_ALIGN - 1) / CUDA_Q8_0_NE_ALIGN  block=WARP_SIZE  smem=0  stream=stream
  smem (1):
    __shared__ int vals[nint];
  syncs:    1×
  callees (1):
    __half2half2 ×1
```

### `cumsum.cu`

```
=== cumsum_cub_kernel (cumsum.cu:12) ===
  attrs:    __global__ static
  launches:
    grid=ne01, ne02, ne03  block=CUDA_CUMSUM_BLOCK_SIZE  smem=0  stream=stream
  smem (2):
    __shared__ typename BlockScanT::TempStorage temp_storage;
    __shared__ T block_carry;
  syncs:    3×
  callees (2):
    BlockScanT ×1
    InclusiveSum ×1
```

```
=== cumsum_kernel (cumsum.cu:86) ===
  attrs:    __global__ static
  launches:
    grid=ne01, ne02, ne03  block=block_size, 1, 1  smem=(block_size + warps_per_block + 2) * sizeof(float)  stream=stream
  smem (1):
    extern __shared__ float smem[];
  syncs:    4×
  callees (3):
    ggml_cuda_get_physical_warp_size ×1
    ggml_cuda_cast ×2
    warp_prefix_inclusive_sum ×2
```

### `solve_tri.cu`

```
=== solve_tri_f32_fast (solve_tri.cu:92) ===
  attrs:    __global__ static
  template (2):
    n_template : int
    k_template : int
  smem (1):
    __shared__ float sA[MAX_N_FAST * MAX_N_FAST];
  syncs:    1×
  callees (2):
    fast_div_modulo ×1
    warp_reduce_sum ×2
```

### `mmvf.cu`

```
=== mul_mat_vec_f (mmvf.cu:8) ===
  attrs:    __global__ static
  template (6):
    T : typename
    type_acc : typename
    ncols_dst : int
    block_size : int
    has_fusion = false : bool
    is_multi_token_id = false : bool
  launches:
    grid=block_nums  block=block_dims  smem=nbytes_shared  stream=stream
    grid=block_nums  block=block_dims  smem=nbytes_shared  stream=stream
  smem (1):
    extern __shared__ char data_mmv[];
  syncs:    3×
  callees (7):
    ggml_cuda_get_physical_warp_size ×1
    ggml_cuda_mad ×16
    ggml_cuda_cast ×4
    warp_reduce_sum ×4
    ggml_cuda_op_silu_single ×1
    ggml_cuda_op_gelu_single ×1
    ggml_cuda_op_swiglu_oai_single ×1
```

### `cpy.cu`

```
=== cpy_scalar_transpose (cpy.cu:43) ===
  attrs:    __global__ static
  template (1):
    T : typename
  launches:
    grid=grid_x, grid_y, grid_z  block=CUDA_CPY_TILE_DIM_2D, CUDA_CPY_BLOCK_ROWS, 1  smem=0  stream=stream
  smem (1):
    __shared__ float tile[2][CUDA_CPY_TILE_DIM_2D][CUDA_CPY_TILE_DIM_2D+1];
  syncs:    1×
```

### `ssm-conv.cu`

```
=== ssm_conv_long_token_f32 (ssm-conv.cu:50) ===
  attrs:    __global__ static
  template (4):
    apply_silu : bool
    split_d_inner : size_t
    d_conv : size_t
    split_n_t : int64_t
  launches:
    grid=n_s, (nr + threads - 1) / threads, 1  block=128  smem=threads * (kNC - 1 + split_n_t) * sizeof(float)  stream=stream
  smem (1):
    extern __shared__ float smem[];
  syncs:    1×
  callees (1):
    ggml_cuda_op_silu_single ×1
```

### `mmid.cu`

```
=== mm_ids_helper (mmid.cu:28) ===
  attrs:    __device__ static __global__
  template (1):
    n_expert_used_template : int
  occ:      max_threads=ggml_cuda_get_physical_warp_size(), min_blocks_per_sm=1
  launches:
    grid=n_experts, 1, 1  block=warp_size, 1, 1  smem=n_tokens*sizeof(mm_ids_helper_store)  stream=stream
  smem (1):
    extern __shared__ char data_mm_ids_helper[];
  loops (4):
    [?] for (int iex = threadIdx.x; iex < n_expert_used; iex += warp_size) {
    [?] for (int it0 = 0; it0 < n_tokens; it0 += warp_size/neu_padded) {
    [?, unroll] for (int offset = neu_padded; offset < warp_size; offset += neu_padded) {
    [?] for (int itc = threadIdx.x; itc < it_compact; itc += warp_size) {
  callees (6):
    ggml_cuda_get_physical_warp_size ×1
    mm_ids_helper_store ×2
    warp_reduce_any ×2
    warp_reduce_sum ×1
    it ×1
    iex_used ×1
```

### `fattn-vec.cuh`

```
=== flash_attn_ext_vec (fattn-vec.cuh:21) ===
  attrs:    static __device__ __global__
  occ:      max_threads=ggml_cuda_fattn_vec_get_nthreads_device(), min_blocks_per_sm=1
  launches:
    grid=blocks_num  block=warp_size, nwarps, 1  smem=nbytes_shared  stream=main_stream
  smem (4):
    __shared__ half   KQ[ne_KQ > ne_combine ? ne_KQ : ne_combine];
    __shared__ float  KQ[ne_KQ > ne_combine ? ne_KQ : ne_combine];
    __shared__ float KQ_max_shared[ncols][WARP_SIZE];
    __shared__ float KQ_sum_shared[ncols][WARP_SIZE];
  loops (3):
    [ncols, unroll] for (int j0 = 0; j0 < ncols; j0 += nwarps) {
    [ncols, unroll] for (int j0 = 0; j0 < ncols; j0 += nwarps) {
    [warp, unroll] for (int w = 0; w < nwarps; ++w) {
  syncs:    6×
  callees (12):
    ggml_cuda_get_max_cpy_bytes ×1
    ggml_cuda_fattn_vec_get_nthreads_device ×1
    get_vec_dot_KQ ×1
    get_dequantize_V ×2
    get_alibi_slope ×1
    quantize_q8_1_to_shared ×1
    ggml_cuda_memcpy_1 ×7
    vec_dot_KQ ×1
    warp_reduce_sum ×3
    __half2half2 ×1
    dequantize_V ×3
    warp_reduce_max ×1
```

### `argsort.cu`

```
=== k_argsort_f32_i32 (argsort.cu:119) ===
  attrs:    __device__ static inline __global__
  smem (1):
    extern __shared__ int dst_row[];
  syncs:    2×
  callees (1):
    ggml_cuda_swap ×2
```

### `wkv.cu`

```
=== rwkv_wkv_f32 (wkv.cu:5) ===
  attrs:    __global__ static
  template (1):
    block_size : int
  launches:
    grid=B * H  block=C / H  smem=0  stream=stream
    grid=B * H  block=C / H  smem=0  stream=stream
  smem (1):
    __shared__ float _k[head_size], _r[head_size], _tf[head_size], _td[head_size];
  syncs:    4×
```

```
=== rwkv_wkv7_f32 (wkv.cu:69) ===
  attrs:    __global__ static
  template (1):
    block_size : int
  launches:
    grid=B * H  block=C / H  smem=0  stream=stream
    grid=B * H  block=C / H  smem=0  stream=stream
  smem (1):
    __shared__ float _r[head_size], _w[head_size], _k[head_size], _a[head_size], _b[head_size];
  syncs:    2×
```

### `fattn-common.cuh`

```
=== flash_attn_mask_to_KV_max (fattn-common.cuh:626) ===
  attrs:    static __device__ __global__
  template (1):
    ncols1 : int
  occ:      max_threads=FATTN_KQ_STRIDE/2, min_blocks_per_sm=1
  launches:
    grid=ntiles_x, Q->ne[3], 1  block=FATTN_KQ_STRIDE/2, 1, 1  smem=0  stream=main_stream
  smem (1):
    __shared__ int buf_iw[WARP_SIZE];
  syncs:    3×
  callees (2):
    isinf ×1
    warp_reduce_all ×2
```

```
=== flash_attn_stream_k_fixup (fattn-common.cuh:679) ===
  attrs:    __global__ static
  occ:      max_threads=D, min_blocks_per_sm=1
```

```
=== flash_attn_combine_results (fattn-common.cuh:778) ===
  attrs:    __global__ static
  occ:      max_threads=D, min_blocks_per_sm=1
  smem (1):
    extern __shared__ float2 meta[];
  syncs:    1×
```

### `gated_delta_net.cu`

```
=== gated_delta_net_cuda (gated_delta_net.cu:4) ===
  attrs:    __global__
  template (2):
    S_v : int
    KDA : bool
  occ:      max_threads=(ggml_cuda_get_physical_warp_size() < S_v ? ggml_cuda_get_physical_warp_size() : S_v) * 4, 2
  launches:
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
  callees (3):
    __launch_bounds__ ×1
    ggml_cuda_get_physical_warp_size ×2
    warp_reduce_sum ×4
```

### `fattn-mma-f16.cuh`

```
=== flash_attn_ext_f16_load_tile [device] (fattn-mma-f16.cuh:304) ===
  attrs:    __host__ static __device__ __forceinline__ inline
  loops (2):
    [?, unroll] for (int i0 = 0; i0 < nbatch_fa; i0 += nwarps*stride_i) {
    [?, unroll] for (int i0 = 0; i0 < nbatch_fa; i0 += nwarps*stride_i) {
  callees (4):
    ggml_cuda_get_physical_warp_size ×1
    ggml_cuda_cvta_generic_to_shared ×1
    __device__ ×2
    cp_async_cg_16 ×1
```

```
=== flash_attn_ext_f16_load_mask [device] (fattn-mma-f16.cuh:387) ===
  attrs:    __device__ __forceinline__ static inline
  loops (4):
    [?, unroll] for (int j1 = 0; j1 < ncols1; j1 += nwarps) {
    [?, unroll] for (int i0 = 0; i0 < nbatch_fa; i0 += warp_size) {
    [?, unroll] for (int j1 = 0; j1 < ncols1; j1 += nwarps) {
    [?, unroll] for (int i0 = 0; i0 < nbatch_fa; i0 += 2*warp_size) {
  callees (4):
    ggml_cuda_get_physical_warp_size ×1
    ggml_cuda_cvta_generic_to_shared ×1
    cp_async_cg_16 ×1
    ggml_cuda_memcpy_1 ×2
```

```
=== flash_attn_ext_f16_process_tile [device] (fattn-mma-f16.cuh:1006) ===
  attrs:    __device__ __forceinline__ static inline
  smem (1):
    extern __shared__ half2 tile_Q[];
  loops (6):
    [?, unroll] for (int stride_k : {warp_size, warp_size/2, warp_size/4, warp_size/8}) {
    [?, unroll] for (int jc0 = 0; jc0 < ncols; jc0 += nwarps*stride_jc) {
    [K-outer] for (; kb0 < kb0_stop-1; ++kb0) {
    [K-outer] for (; kb0 < kb0_stop-1; ++kb0) {
    [?, unroll] for (int stride_k : {warp_size, warp_size/2, warp_size/4, warp_size/8}) {
    [?, unroll] for (int jc0_dst = 0; jc0_dst < ncols; jc0_dst += (nwarps/np)*stride_jc) {
  syncs:    12×
  callees (14):
    ggml_cuda_get_physical_warp_size ×1
    get_cols_per_thread ×1
    ggml_cuda_fattn_mma_get_nbatch_fa ×1
    ggml_cuda_fattn_mma_get_nbatch_K2 ×1
    ggml_cuda_fattn_mma_get_nbatch_V2 ×1
    ggml_cuda_fattn_mma_get_nbatch_combine ×1
    ggml_cuda_fattn_mma_get_Q_in_reg ×1
    ggml_cuda_fattn_mma_get_nstages ×1
    load_ldmatrix ×1
    flash_attn_ext_f16_load_mask ×1
    flash_attn_ext_f16_load_tile ×1
    get_j ×5
    get_i ×6
    get_transposed ×1
```

```
=== flash_attn_ext_f16 (fattn-mma-f16.cuh:1549) ===
  attrs:    __global__ static
  occ:      max_threads=ggml_cuda_fattn_mma_get_nthreads(DKQ, DV, ncols1*ncols2), min_blocks_per_sm=ggml_cuda_fattn_mma_get_occupancy(DKQ, DV, ncols1*ncols2)
  launches:
    grid=blocks_num  block=warp_size, nwarps, 1  smem=nbytes_shared  stream=main_stream
  callees (5):
    ggml_cuda_get_physical_warp_size ×1
    ggml_cuda_fattn_mma_get_nbatch_fa ×1
    ggml_cuda_fattn_mma_get_nthreads ×1
    get_alibi_slope ×2
    flash_attn_ext_f16_process_tile ×3
```

### `mmq.cuh`

```
=== mul_mat_q_process_tile [device] (mmq.cuh:3443) ===
  attrs:    static __device__ __forceinline__ inline
  template (4):
    type : ggml_type
    mmq_x : int
    need_check : bool
    fixup : bool
  smem (1):
    extern __shared__ int data_mul_mat_q[];
  loops (3):
    [K-outer] for (int kb0 = kb0_start; kb0 < kb0_stop; kb0 += blocks_per_iter) {
    [tile-coop, unroll] for (int l0 = 0; l0 < mmq_x * MMQ_TILE_Y_K; l0 += nwarps * warp_size) {
    [tile-coop, unroll] for (int l0 = 0; l0 < mmq_x * MMQ_TILE_Y_K; l0 += nwarps * warp_size) {
  syncs:    4×
  callees (8):
    ggml_cuda_get_physical_warp_size ×1
    mmq_get_nwarps_device ×1
    get_mmq_y_device ×1
    GGML_PAD ×1
    get_iter_k ×1
    load_tiles ×1
    vec_dot ×2
    write_back ×2
```

```
=== mul_mat_q (mmq.cuh:3538) ===
  attrs:    __global__ static
  template (3):
    type : ggml_type
    mmq_x : int
    need_check : bool
  occ:      max_threads=ggml_cuda_get_physical_warp_size()*mmq_get_nwarps_device(), min_blocks_per_sm=2
  launches:
    grid=nty, ntx, ntzw  block=warp_size, nwarps, 1  smem=mmq_get_nbytes_shared<type>(mmq_x, mmq_y, cc, warp_size, nwarps)  stream=stream
    grid=nty, ntx, ntzw  block=warp_size, nwarps, 1  smem=mmq_get_nbytes_shared<type>(mmq_x, mmq_y, cc, warp_size, nwarps)  stream=stream
    grid=nsm, 1, 1  block=warp_size, nwarps, 1  smem=mmq_get_nbytes_shared<type>(mmq_x, mmq_y, cc, warp_size, nwarps)  stream=stream
    grid=nsm, 1, 1  block=warp_size, nwarps, 1  smem=mmq_get_nbytes_shared<type>(mmq_x, mmq_y, cc, warp_size, nwarps)  stream=stream
  smem (1):
    extern __shared__ int ids_dst_shared[]; // Stored at beginning of shared memory.
  loops (4):
    [Y-coop, unroll] for (int j0 = 0; j0 < mmq_x; j0 += nwarps*warp_size) {
    [Y-coop, unroll] for (int j0 = 0; j0 < mmq_x; j0 += nwarps*warp_size) {
    [Y-coop, unroll] for (int j0 = 0; j0 < mmq_x; j0 += nwarps*warp_size) {
    [Y-coop, unroll] for (int j0 = 0; j0 < mmq_x; j0 += nwarps*warp_size) {
  syncs:    7×
  callees (7):
    get_mmq_x_max_device ×1
    mmq_get_granularity_device ×1
    mmq_get_nwarps_device ×1
    ggml_cuda_get_physical_warp_size ×1
    get_mmq_y_device ×1
    mul_mat_q_process_tile ×3
    get_iter_k ×1
```

```
=== mul_mat_q_stream_k_fixup (mmq.cuh:3780) ===
  attrs:    __global__ static
  template (3):
    type : ggml_type
    mmq_x : int
    need_check : bool
  launches:
    grid=nsm, 1, 1  block=warp_size, nwarps, 1  smem=0  stream=stream
    grid=nsm, 1, 1  block=warp_size, nwarps, 1  smem=0  stream=stream
  smem (1):
    __shared__ int ids_dst_shared[mmq_x];
  loops (7):
    [?, unroll] for (int j0 = 0; j0 < mmq_x; j0 += nwarps) {
    [?, unroll] for (int i0 = 0; i0 < mmq_y; i0 += warp_size) {
    [?, unroll] for (int j0 = 0; j0 < mmq_x; j0 += nwarps) {
    [?, unroll] for (int i0 = 0; i0 < mmq_y; i0 += warp_size) {
    [col] for (int j = threadIdx.y*warp_size + threadIdx.x; j < mmq_x; j += nwarps*warp_size) {
    [?, unroll] for (int j0 = 0; j0 < mmq_x; j0 += nwarps) {
    [?, unroll] for (int i0 = 0; i0 < mmq_y; i0 += warp_size) {
  syncs:    1×
  callees (4):
    get_mmq_y_device ×1
    get_iter_k ×1
    mmq_get_nwarps_device ×1
    ggml_cuda_get_physical_warp_size ×1
```

### `ssm-scan.cu`

```
=== ssm_scan_f32 (ssm-scan.cu:19) ===
  attrs:    __global__
  template (3):
    splitD : size_t
    N : size_t
    L_template : size_t
  occ:      max_threads=splitD, min_blocks_per_sm=1
  launches:
    grid=n_seq, (n_head + threads - 1) / threads, 1  block=128  smem=(threads * (d_state + 1) * 2) * sizeof(float)  stream=stream
    grid=n_seq, (n_head + threads - 1) / threads, 1  block=128  smem=(threads * (d_state + 1) * 2) * sizeof(float)  stream=stream
    grid=n_seq, (n_head + threads - 1) / threads, 1  block=128  smem=(threads * (d_state + 1) * 2) * sizeof(float)  stream=stream
    grid=n_seq, (n_head + threads - 1) / threads, 1  block=128  smem=(threads * (d_state + 1) * 2) * sizeof(float)  stream=stream
    grid=n_seq, (n_head + threads - 1) / threads, 1  block=128  smem=(threads * (d_state + 1) * 2) * sizeof(float)  stream=stream
    grid=n_seq, (n_head + threads - 1) / threads, 1  block=128  smem=(threads * (d_state + 1) * 2) * sizeof(float)  stream=stream
    grid=n_seq, (n_head + threads - 1) / threads, 1  block=128  smem=(threads * (d_state + 1) * 2) * sizeof(float)  stream=stream
    grid=n_seq, (n_head + threads - 1) / threads, 1  block=128  smem=(threads * (d_state + 1) * 2) * sizeof(float)  stream=stream
    grid=n_seq, (n_head + threads - 1) / threads, 1  block=128  smem=(threads * (d_state + 1) * 2) * sizeof(float)  stream=stream
  smem (3):
    __shared__ float smemB[N];
    __shared__ float smemC[N];
    __shared__ CubTempStorage cub_temp_storage;
  syncs:    1×
  callees (6):
    __launch_bounds__ ×1
    BlockLoad ×2
    Load ×2
    log1pf ×1
    BlockStore ×1
    Store ×1
```

```
=== ssm_scan_f32_group (ssm-scan.cu:118) ===
  attrs:    __global__
  template (2):
    c_factor : int
    d_state : int
  occ:      max_threads=d_state, min_blocks_per_sm=1
  launches:
    grid=(n_head * head_dim + (num_warps - 1)) / num_warps, n_seq, 1  block=threads  smem=0  stream=stream
    grid=(n_head * head_dim + (num_warps - 1)) / num_warps, n_seq, 1  block=threads  smem=0  stream=stream
  callees (3):
    __launch_bounds__ ×1
    log1pf ×1
    warp_reduce_sum ×1
```

### `topk-moe.cu`

```
=== topk_moe_cuda (topk-moe.cu:80) ===
  attrs:    __device__ __global__
  template (2):
    n_experts : int
    has_bias : bool
  occ:      max_threads=4 * WARP_SIZE, min_blocks_per_sm=1
  launches:
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
    grid=grid_dims  block=block_dims  smem=0  stream=stream
  callees (5):
    __launch_bounds__ ×1
    sigmoid_warp_inplace ×1
    softmax_warp_inplace ×2
    __isnanf ×1
    warp_reduce_sum ×1
```

### `argmax.cu`

```
=== argmax_f32 (argmax.cu:8) ===
  attrs:    __global__ static
  launches:
    grid=num_blocks, 1, 1  block=num_threads, 1, 1  smem=0  stream=stream
  smem (2):
    __shared__ float shared_maxval[max_warps];
    __shared__ int   shared_argmax[max_warps];
  syncs:    1×
```

### `reduce_rows.cuh`

```
=== reduce_rows_f32 (reduce_rows.cuh:5) ===
  attrs:    __global__ static
  template (1):
    norm : bool
  launches:
    grid=nrows, 1, 1  block=512, 1, 1  smem=0  stream=stream
    grid=nrows, 1, 1  block=ncols < 1024 ? 32 : 128, 1, 1  smem=0  stream=stream
    grid=nrows, 1, 1  block=512, 1, 1  smem=0  stream=stream
    grid=nrows, 1, 1  block=ncols < 1024 ? 32 : 128, 1, 1  smem=0  stream=stream
    grid=nrows, 1, 1  block=512, 1, 1  smem=0  stream=stream
    grid=nrows, 1, 1  block=ncols < 1024 ? 32 : 128, 1, 1  smem=0  stream=stream
  smem (1):
    __shared__ float shared_vals[32];
  callees (1):
    block_reduce ×1
```

### `norm.cu`

```
=== norm_f32 (norm.cu:5) ===
  attrs:    __global__ static
  template (1):
    block_size : int
  launches:
    grid=nrows, nchannels, nsamples  block=WARP_SIZE, 1, 1  smem=0  stream=stream
    grid=nrows, nchannels, nsamples  block=1024, 1, 1  smem=block_dims.x > WARP_SIZE ? 32 * sizeof(float2): 0, stream  stream=default
    grid=num_groups  block=WARP_SIZE, 1, 1  smem=0  stream=stream
    grid=num_groups  block=1024, 1, 1  smem=block_dims.x > WARP_SIZE ? 32 * sizeof(float): 0, stream  stream=default
    grid=nrows, nchannels, nsamples  block=256, 1, 1  smem=block_dims.x > WARP_SIZE ? 32 * sizeof(float): 0, stream  stream=default
    grid=nrows, nchannels, nsamples  block=1024, 1, 1  smem=block_dims.x > WARP_SIZE ? 32 * sizeof(float): 0, stream  stream=default
    grid=nrows, nchannels, nsamples  block=256, 1, 1  smem=block_dims.x > WARP_SIZE ? 32 * sizeof(float): 0, stream  stream=default
    grid=nrows, nchannels, nsamples  block=1024, 1, 1  smem=block_dims.x > WARP_SIZE ? 32 * sizeof(float): 0, stream  stream=default
    grid=nrows, nchannels, nsamples  block=256, 1, 1  smem=block_dims.x > WARP_SIZE ? 32 * sizeof(float): 0, stream  stream=default
    grid=nrows, nchannels, nsamples  block=1024, 1, 1  smem=block_dims.x > WARP_SIZE ? 32 * sizeof(float): 0, stream  stream=default
    grid=nrows, nchannels, nsamples  block=WARP_SIZE, 1, 1  smem=0  stream=stream
    grid=nrows, nchannels, nsamples  block=1024, 1, 1  smem=block_dims.x > WARP_SIZE ? 32 * sizeof(float): 0, stream  stream=default
  smem (1):
    extern __shared__ float2 s_sum2[];
  callees (1):
    block_reduce ×1
```

```
=== group_norm_f32 (norm.cu:41) ===
  attrs:    extern __global__ static
  template (1):
    block_size : int
  smem (1):
    extern __shared__ float s_sum[];
  callees (1):
    block_reduce ×2
```

```
=== rms_norm_f32 (norm.cu:75) ===
  attrs:    extern __global__ static
  template (3):
    block_size : int
    do_multiply = false : bool
    do_add = false : bool
  smem (1):
    extern __shared__ float s_sum[];
  callees (1):
    block_reduce ×1
```

```
=== rms_norm_back_f32 (norm.cu:154) ===
  attrs:    extern __global__ static
  template (1):
    block_size : int
  launches:
    grid=nrows  block=WARP_SIZE, 1, 1  smem=0  stream=stream
    grid=nrows  block=1024, 1, 1  smem=0  stream=stream
  smem (2):
    __shared__ float s_sum_xx[32];
    __shared__ float s_sum_xg[32];
  syncs:    1×
  callees (1):
    warp_reduce_sum ×4
```

```
=== l2_norm_f32 (norm.cu:206) ===
  attrs:    static __global__
  template (1):
    block_size : int
  smem (1):
    //         __shared__ float s_sum[32];
  syncs:    1×
```

```
=== l2_norm_f32 (norm.cu:240) ===
  attrs:    __global__ static
  template (1):
    block_size : int
  smem (1):
    extern __shared__ float s_sum[];
  callees (1):
    block_reduce ×1
```

### `fattn-wmma-f16.cu`

```
=== flash_attn_ext_f16 (fattn-wmma-f16.cu:26) ===
  attrs:    __global__ static
  occ:      max_threads=nwarps*ggml_cuda_get_physical_warp_size(), min_blocks_per_sm=1
  launches:
    grid=blocks_num  block=warp_size, nwarps, 1  smem=nbytes_shared  stream=main_stream
  smem (2):
    __shared__ half KQ[mem_KQ >= mem_VKQ_parts ? mem_KQ : mem_VKQ_parts];
    __shared__ half VKQ[ncols*D_padded]; // Accumulator for final VKQ slice.
  loops (20):
    [?, unroll] for (int j = 0; j < ncols/nwarps; ++j) {
    [?, unroll] for (int j = 0; j < ncols/nwarps; ++j) {
    [ncols, unroll] for (int j0 = 0; j0 < ncols; j0 += nwarps) {
    [?, unroll] for (int i0 = 0; i0 < D/2; i0 += warp_size) {
    [ncols, unroll] for (int j0 = 0; j0 < ncols; j0 += nwarps) {
    [?, unroll] for (int i0 = 0; i0 < D; i0 += warp_size) {
    [ncols, unroll] for (int j0 = 0; j0 < ncols; j0 += nwarps) {
    [?, unroll] for (int k0 = 0; k0 < FATTN_KQ_STRIDE; k0 += warp_size) {
    [?, unroll] for (int k0 = 0; k0 < FATTN_KQ_STRIDE; k0 += warp_size) {
    [?, unroll] for (int k0 = 0; k0 < FATTN_KQ_STRIDE; k0 += warp_size) {
    [?, unroll] for (int k0 = 0; k0 < FATTN_KQ_STRIDE/2; k0 += warp_size) {
    [?, unroll] for (int k0 = 0; k0 < FATTN_KQ_STRIDE/2; k0 += warp_size) {
    [?, unroll] for (int k0 = 0; k0 < FATTN_KQ_STRIDE/2; k0 += warp_size) {
    [ncols, unroll] for (int j0 = 0; j0 < ncols; j0 += nwarps) {
    [?, unroll] for (int i0 = 0; i0 < D/2; i0 += warp_size) {
    [ncols, unroll] for (int j0 = 0; j0 < ncols; j0 += nwarps) {
    [?, unroll] for (int i0 = 0; i0 < D/2; i0 += warp_size) {
    [?, unroll] for (int i0 = 0; i0 < D/2; i0 += warp_size) {
    [ncols, unroll] for (int j0 = 0; j0 < ncols; j0 += nwarps) {
    [?, unroll] for (int i0 = 0; i0 < D; i0 += warp_size) {
  syncs:    8×
  callees (14):
    ggml_cuda_get_physical_warp_size ×1
    get_alibi_slope ×1
    load_matrix_sync ×4
    fill_fragment ×2
    mma_sync ×2
    store_matrix_sync ×2
    warp_reduce_max ×2
    warp_reduce_sum ×2
    h2exp ×3
    ggml_cuda_hmax2 ×1
    __half2half2 ×3
    ggml_cuda_hmax ×1
    __hgt2_mask ×2
    hexp ×2
```

### `mmf.cuh`

```
=== mul_mat_f (mmf.cuh:50) ===
  attrs:    __forceinline__ static inline __device__ __global__
  template (5):
    T : typename
    rows_per_block : int
    cols_per_block : int
    nwarps : int
    has_ids : bool
  occ:      max_threads=ggml_cuda_get_physical_warp_size()*nwarps, min_blocks_per_sm=1
  launches:
    grid=block_nums  block=block_dims  smem=nbytes_shared_total  stream=stream
    grid=block_nums  block=block_dims  smem=nbytes_shared_total  stream=stream
```

```
=== mul_mat_f_ids (mmf.cuh:299) ===
  attrs:    __global__ static
  template (4):
    T : typename
    rows_per_block : int
    cols_per_block : int
    nwarps : int
  occ:      max_threads=ggml_cuda_get_physical_warp_size()*nwarps, min_blocks_per_sm=1
  launches:
    grid=block_nums.x, ids_data->n_experts, max_tiles  block=block_dims  smem=nbytes_shared_total  stream=stream
```

### `mmvq.cu`

```
=== mul_mat_vec_q (mmvq.cu:391) ===
  attrs:    __device__ __host__ static __global__
  template (4):
    type : ggml_type
    ncols_dst : int
    has_fusion : bool
    small_k = false : bool
  occ:      max_threads=calc_nwarps(type, ncols_dst, get_device_table_id())*ggml_cuda_get_physical_warp_size(), min_blocks_per_sm=1
  launches:
    grid=nblocks, nchannels_dst, nsamples_or_ntokens  block=warp_size, nwarps, 1  smem=nbytes_shared  stream=stream
    grid=nblocks, nchannels_dst, nsamples_or_ntokens  block=warp_size, nwarps, 1  smem=nbytes_shared  stream=stream
  smem (2):
    __shared__ float tmp_shared[nwarps-1 > 0 ? nwarps-1 : 1][ncols_dst][rows_per_cuda_block][warp_size];
    __shared__ float tmp_shared_gate[(has_fusion && (nwarps-1 > 0)) ? nwarps-1 : 1][ncols_dst][rows_per_cuda_block][warp_size];
  loops (2):
    [K-outer] for (int kbx = tid / (qi/vdr); kbx < blocks_per_row_x; kbx += blocks_per_iter) {
    [?, unroll] for (int l = 0; l < nwarps-1; ++l) {
  syncs:    1×
  callees (11):
    get_vdr_mmvq ×1
    get_device_table_id ×1
    calc_nwarps ×1
    calc_rows_per_block ×1
    ggml_cuda_get_physical_warp_size ×1
    get_vec_dot_q_cuda ×1
    vec_dot_q_cuda ×2
    warp_reduce_sum ×2
    ggml_cuda_op_silu_single ×1
    ggml_cuda_op_gelu_single ×1
    ggml_cuda_op_swiglu_oai_single ×1
```

```
=== mul_mat_vec_q_moe (mmvq.cu:597) ===
  attrs:    __global__ static
  template (2):
    type : ggml_type
    c_rows_per_block : int
  occ:      max_threads=get_mmvq_mmid_max_batch_for_device<type>()*ggml_cuda_get_physical_warp_size(), min_blocks_per_sm=1
  launches:
    grid=nblocks_rows, nchannels_dst  block=warp_size, ncols_dst  smem=0  stream=stream
  loops (1):
    [K-outer] for (int kbx = threadIdx.x / (qi/vdr); kbx < blocks_per_row_x; kbx += blocks_per_iter) {
  callees (5):
    get_vdr_mmvq ×1
    ggml_cuda_get_physical_warp_size ×1
    get_vec_dot_q_cuda ×1
    vec_dot_q_cuda ×1
    warp_reduce_sum ×1
```

### `softmax.cu`

```
=== soft_max_f32 (softmax.cu:55) ===
  attrs:    __global__ static
  template (4):
    use_shared : bool
    ncols_template : int
    block_size_template : int
    T : typename
  launches:
    grid=block_nums  block=block_dims  smem=nbytes_shared  stream=stream
    grid=block_nums  block=block_dims  smem=nbytes_shared  stream=stream
  smem (1):
    extern __shared__ float data_soft_max_f32[];
  callees (3):
    get_alibi_slope ×1
    t2f32 ×1
    block_reduce ×2
```

```
=== soft_max_f32_parallelize_cols_single_row [device] (softmax.cu:141) ===
  attrs:    __device__ static
  smem (1):
    __shared__ float shared_vals[32];
  callees (4):
    this_grid ×1
    block_reduce ×4
    sync ×2
    assert ×1
```

```
=== soft_max_f32_parallelize_cols (softmax.cu:302) ===
  attrs:    static __global__
  occ:      max_threads=8*WARP_SIZE, min_blocks_per_sm=1
  callees (2):
    __launch_bounds__ ×1
    soft_max_f32_parallelize_cols_single_row ×1
```

### `concat.cu`

```
=== concat_f32_non_cont (concat.cu:98) ===
  attrs:    static __global__
  template (1):
    dim : int
  occ:      max_threads=CUDA_CONCAT_BLOCK_SIZE
  launches:
    grid=dst->ne[1], dst->ne[2], dst->ne[3]  block=CUDA_CONCAT_BLOCK_SIZE  smem=0  stream=stream
  callees (1):
    __launch_bounds__ ×1
```

### `quantize.cu`

```
=== quantize_q8_1 (quantize.cu:5) ===
  attrs:    __global__ static
  occ:      max_threads=CUDA_QUANTIZE_BLOCK_SIZE, min_blocks_per_sm=1
  launches:
    grid=block_num_x, ne1, ne2*ne3  block=CUDA_QUANTIZE_BLOCK_SIZE, 1, 1  smem=0  stream=stream
  callees (3):
    warp_reduce_max ×1
    warp_reduce_sum ×1
    roundf ×1
```

### `common.cuh`

```
=== block_reduce [device] (common.cuh:593) ===
  attrs:    static __device__
  template (3):
    reduce_method_t : block_reduce_method
    block_size_template = 0 : const unsigned int
    T : typename
  syncs:    1×
  callees (3):
    reduce ×2
    assert ×1
    sentinel ×1
```

### `gla.cu`

```
=== gated_linear_attn_f32 (gla.cu:5) ===
  attrs:    __global__ static
  launches:
    grid=B * H  block=C / H  smem=0  stream=stream
    grid=B * H  block=C / H  smem=0  stream=stream
  smem (1):
    __shared__ float _k[head_size], _r[head_size], _td[head_size];
  syncs:    2×
```

### `cross-entropy-loss.cu`

```
=== cross_entropy_loss_f32 (cross-entropy-loss.cu:9) ===
  attrs:    __global__ static
  template (1):
    use_shared : bool
  launches:
    grid=nrows, 1, 1  block=WARP_SIZE, 1, 1  smem=ne00*sizeof(float)  stream=stream
    grid=nrows, 1, 1  block=WARP_SIZE, 1, 1  smem=0  stream=stream
  smem (1):
    extern __shared__ float tmp[];
  callees (2):
    warp_reduce_max ×1
    warp_reduce_sum ×2
```

```
=== cross_entropy_loss_back_f32 (cross-entropy-loss.cu:53) ===
  attrs:    __global__ static
  template (1):
    use_shared : bool
  launches:
    grid=nrows, 1, 1  block=WARP_SIZE, 1, 1  smem=ne00*sizeof(float)  stream=stream
    grid=nrows, 1, 1  block=WARP_SIZE, 1, 1  smem=0  stream=stream
  smem (1):
    extern __shared__ float tmp[];
  callees (2):
    warp_reduce_max ×1
    warp_reduce_sum ×1
```

### `fattn-tile.cuh`

```
=== flash_attn_tile_load_tile [device] (fattn-tile.cuh:343) ===
  attrs:    __device__ static __host__ __forceinline__ inline
  loops (1):
    [?, unroll] for (int i0 = 0; i0 < I; i0 += nwarps*stride_i) {
  callees (3):
    ggml_cuda_get_max_cpy_bytes ×1
    __device__ ×1
    ggml_cuda_memcpy_1 ×1
```

```
=== flash_attn_tile_load_tile [device] (fattn-tile.cuh:393) ===
  attrs:    static __device__ __forceinline__ inline
  loops (1):
    [?, unroll] for (int i0 = 0; i0 < I; i0 += nwarps*stride_i) {
  callees (3):
    ggml_cuda_get_max_cpy_bytes ×1
    __device__ ×1
    ggml_cuda_memcpy_1 ×2
```

```
=== flash_attn_tile_iter [device] (fattn-tile.cuh:525) ===
  attrs:    __device__ __forceinline__ static inline
  template (8):
    warp_size : int
    nwarps : int
    ncols1 : int
    ncols2 : int
    DKQ : int
    DV : int
    nbatch_fa : int
    nbatch_K : int
  smem (1):
    __shared__ float KQ_max_new_shared[nwarps];
  loops (9):
    [?, unroll] for (int i_KQ_0 = 0; i_KQ_0 < nbatch_fa; i_KQ_0 += np*warp_size) {
    [?, unroll] for (int i0 = 0; i0 < nbatch_fa; i0 += np*warp_size) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp/2; i0 += warp_size) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp/2; i0 += warp_size) {
    [?, unroll] for (int i0 = 0; i0 < nbatch_fa; i0 += np*warp_size) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp/2; i0 += warp_size*cpy_ne_D) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp/2; i0 += warp_size) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp; i0 += warp_size*cpy_ne_D) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp/2; i0 += warp_size) {
  syncs:    4×
  callees (6):
    ggml_cuda_get_max_cpy_bytes ×1
    flash_attn_tile_iter_KQ ×2
    warp_reduce_max ×2
    ggml_cuda_memcpy_1 ×5
    flash_attn_tile_load_tile ×1
    __half2half2 ×1
```

```
=== flash_attn_tile (fattn-tile.cuh:758) ===
  attrs:    __global__ static
  occ:      max_threads=ggml_cuda_fattn_tile_get_nthreads(DKQ, DV, ncols1*ncols2), min_blocks_per_sm=ggml_cuda_fattn_tile_get_occupancy(DKQ, DV, ncols1*ncols2)
  launches:
    grid=blocks_num  block=warp_size, nwarps, 1  smem=nbytes_shared  stream=main_stream
  smem (6):
    __shared__ half2 Q_tmp[ncols * DKQ/2];
    __shared__ half2 KV_tmp[nbatch_fa * (nbatch_K/2 + cpy_ne) + DVp-DV];
    __shared__ half  KQ[ncols * nbatch_fa];
    __shared__ float Q_tmp[ncols * DKQ];
    __shared__ float KV_tmp[nbatch_fa * (nbatch_K + cpy_ne) + DVp-DV];
    __shared__ float KQ[ncols * nbatch_fa];
  loops (10):
    [ncols, unroll] for (int j0 = 0; j0 < ncols; j0 += nwarps) {
    [D-axis, unroll] for (int i0 = 0; i0 < DKQp; i0 += np*warp_size*cpy_ne_D) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp/2; i0 += warp_size*cpy_ne_D) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp; i0 += warp_size*cpy_ne_D) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp/2; i0 += warp_size*cpy_ne_D) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp; i0 += warp_size*cpy_ne_D) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp/2; i0 += warp_size) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp/2; i0 += warp_size) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp/2; i0 += warp_size*cpy_ne_D) {
    [D-axis, unroll] for (int i0 = 0; i0 < DVp; i0 += warp_size*cpy_ne_D) {
  syncs:    2×
  callees (9):
    ggml_cuda_fattn_tile_get_config ×1
    ggml_cuda_fattn_tile_get_nthreads ×1
    ggml_cuda_fattn_tile_get_nbatch_fa ×1
    ggml_cuda_fattn_tile_get_nbatch_K ×1
    get_alibi_slope ×1
    ggml_cuda_get_max_cpy_bytes ×1
    ggml_cuda_memcpy_1 ×9
    flash_attn_tile_iter ×3
    warp_reduce_sum ×1
```

